describe('TC-80 49 Price History - No Data Available', () => { const productId = '67890'; beforeEach(() => { cy.visit(`/product/${productId}`); }); it('should display the product details page', () => { cy.url().should('include', `/product/${productId}`); cy.get('.product-details').should('be.visible'); }); it('should locate the Price History section', () => { cy.get('.price-history-section').should('be.visible'); }); it('should verify the no data available message in Price History section', () => { cy.get('.price-history-section .no-data-message') .should('be.visible') .and('contain.text', 'No price history data available'); }); });
describe('Automatic Discount Application Test', () => { before(() => { cy.visit('https://example.com'); cy.login('validUsername', 'validPassword'); }); it('should apply discount automatically', () => { const productId = 101; const discountCode = 'AUTO10'; cy.navigateToProductPage(productId); cy.addProductToCart(productId); cy.navigateToCart(); cy.verifyDiscountApplied(productId, discountCode); cy.verifyTotalPriceAfterDiscount(productId); cy.proceedToCheckout(); cy.verifyDiscountOnCheckoutPage(productId, discountCode); cy.completePurchase(); }); after(() => { cy.logout(); }); });
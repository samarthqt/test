describe('Account Balance Alert Notification', () => { before(() => { cy.visit('/login'); cy.login('validUser', 'validPassword'); }); it('should ensure account balance is below the set threshold', () => { cy.visit('/account-summary'); cy.get('.account-balance').invoke('text').then((balanceText) => { const balance = parseFloat(balanceText.replace('$', '')); expect(balance).to.be.lessThan(200); }); }); it('should trigger the balance alert notification', () => { cy.triggerBalanceAlert(); cy.get('.notification').should('be.visible'); }); it('should verify user receives a notification', () => { cy.get('.notification-message').should('contain.text', 'Your account balance is below the minimum threshold of $200'); }); after(() => { cy.logout(); }); });
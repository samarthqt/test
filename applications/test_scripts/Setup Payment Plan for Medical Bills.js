describe('Setup Payment Plan for Medical Bills', () => {  before(() => {    cy.visit('/login');    cy.login('validUsername', 'validPassword');  });  it('should navigate to the Billing module', () => {    const billingPage = new BillingPage();    billingPage.navigateToBilling();    billingPage.verifyBillingPage();  });  it('should navigate to Payment Plan setup page', () => {    const billingPage = new BillingPage();    billingPage.selectPaymentPlanSubModule();    billingPage.verifyPaymentPlanPage();  });  it('should enter patient details and create a payment plan', () => {    const paymentPlanPage = new PaymentPlanPage();    paymentPlanPage.enterPatientID('12345');    paymentPlanPage.verifyPatientDetails('$500');    paymentPlanPage.createPaymentPlan();    paymentPlanPage.enterPaymentPlanDuration('6');    paymentPlanPage.enterMonthlyPayment('$83.33');    paymentPlanPage.reviewPaymentPlanDetails();    paymentPlanPage.submitPaymentPlan();    paymentPlanPage.verifyPaymentPlanSubmission();  });  it('should receive confirmation of payment plan approval', () => {    const paymentPlanPage = new PaymentPlanPage();    paymentPlanPage.verifyPaymentPlanApproval();  });});
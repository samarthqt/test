describe('Order Minimum Requirement Failure', () => { before(() => { cy.visit('/login'); cy.login('validUsername', 'validPassword'); }); it('should not apply free shipping if order value is below minimum', () => { cy.searchProduct('54321'); cy.addToCart('54321', 1); cy.proceedToCheckout(); cy.get('.order-summary').should('contain', '$30'); cy.get('.shipping-details').should('not.contain', 'Free Shipping'); cy.completePayment(); cy.get('.order-confirmation').should('contain', 'Standard Shipping Charges'); cy.navigateToOrderHistory(); cy.get('.order-history').should('contain', 'Standard Shipping Charges'); }); after(() => { cy.logout(); }); });
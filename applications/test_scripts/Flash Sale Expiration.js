describe('Flash Sale Expiration Test', () => { before(() => { cy.visit('/product/101'); }); it('should display original price after flash sale ends', () => { cy.get(ProductPage.productPrice).should('not.contain', '20%'); }); it('should add product to cart with original price', () => { cy.get(ProductPage.addToCartButton).click(); cy.get(CartPage.cartPrice).should('not.contain', '20%'); }); it('should display original price at checkout', () => { cy.get(CartPage.proceedToCheckoutButton).click(); cy.get(CheckoutPage.checkoutPrice).should('not.contain', '20%'); }); it('should complete purchase without flash sale discount', () => { cy.get(CheckoutPage.completePurchaseButton).click(); cy.get(OrderSummaryPage.orderTotal).should('not.contain', '20%'); }); it('should verify order summary reflects original price', () => { cy.get(OrderSummaryPage.orderSummaryPrice).should('not.contain', '20%'); }); });
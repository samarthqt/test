describe('Trial Activation with Invalid Email', () => { beforeEach(() => { cy.visit('/login'); }); it('should display an error for invalid email format during login', () => { const loginPage = new LoginPage(); loginPage.enterEmail('invalidemail'); loginPage.submit(); loginPage.getEmailError().should('contain', 'Invalid email format'); }); it('should navigate to Subscription Products and handle invalid email on Free Trial', () => { const loginPage = new LoginPage(); const subscriptionPage = new SubscriptionPage(); loginPage.loginWithValidCredentials(); subscriptionPage.navigateToSubscriptionProducts(); subscriptionPage.selectProductById(303); subscriptionPage.clickFreeTrial(); subscriptionPage.getTrialError().should('contain', 'Please enter a valid email to proceed'); }); });
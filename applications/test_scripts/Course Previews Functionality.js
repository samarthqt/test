describe('Course Previews Functionality', () => { before(() => { cy.login('U001'); }); it('should allow users to preview course content before enrolling', () => { const courseId = '101'; cy.visit('/dashboard'); cy.contains('Course Management Dashboard').should('be.visible'); cy.get(`[data-course-id="${courseId}"]`).click(); cy.contains('Course Details for Course ID 101').should('be.visible'); cy.get('.preview-button').click(); cy.contains('Preview Options').should('be.visible'); cy.get('.preview-video-option').click(); cy.get('.video-player').should('be.visible'); cy.get('.video-player').should('not.have.class', 'paused'); cy.get('.video-player').click(); cy.get('.video-player').should('have.class', 'paused'); cy.get('.preview-lesson-outline-option').click(); cy.contains('Lesson Outline').should('be.visible'); cy.get('.lesson-outline').scrollTo('bottom'); cy.get('.enroll-button').click(); cy.url().should('include', '/enrollment/101'); }); after(() => { cy.logout(); }); });
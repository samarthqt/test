describe('Instant Shipment Tracking Alert for Dispatch', () => { before(() => { cy.visit('https://dynamics365.example.com'); cy.login('validUsername', 'validPassword'); }); it('should verify that customers receive an instant alert when their shipment is dispatched', () => { const ordersPage = new OrdersPage(); ordersPage.navigate(); ordersPage.selectOrderById('12345'); ordersPage.updateShipmentStatusToDispatched(); ordersPage.checkAlertSystem(); ordersPage.verifyCustomerAlert('customer@example.com', 'Your shipment has been dispatched.'); }); after(() => { cy.logout(); }); });
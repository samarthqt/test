describe('Electronic Case Report Forms (eCRF) Submission', () => { beforeEach(() => { cy.visit('/login'); cy.get('#username').type('validUser'); cy.get('#password').type('validPassword'); cy.get('#loginButton').click(); cy.url().should('include', '/dashboard'); }); it('should submit eCRF for a participant', () => { cy.get('#menu').contains('eCRF').click(); cy.url().should('include', '/ecrf'); cy.get('#formList').contains('F67890').click(); cy.get('#name').type('John Doe'); cy.get('#age').type('30'); cy.get('#diagnosis').type('Diagnosis Example'); cy.get('#treatment').type('Treatment Example'); cy.get('#linkParticipant').type('P12345'); cy.get('#submitButton').click(); cy.get('.confirmation').should('contain', 'Form submitted successfully'); cy.get('#participantRecord').contains('P12345').click(); cy.get('.recordData').should('contain', 'John Doe').and('contain', '30').and('contain', 'Diagnosis Example').and('contain', 'Treatment Example'); cy.get('#formStatus').should('contain', 'Submitted'); cy.get('#editFormButton').click(); cy.get('#name').clear().type('Jane Doe'); cy.get('#resubmitButton').click(); cy.get('.confirmation').should('contain', 'Form resubmitted successfully'); cy.get('#auditTrail').should('contain', 'Initial submission').and('contain', 'Subsequent edits'); cy.get('#linkVerification').should('contain', 'P12345'); cy.get('#invalidAccess').type('INVALID_ID').should('have.class', 'errorMessage'); }); afterEach(() => { cy.get('#logoutButton').click(); cy.url().should('include', '/login'); }); });
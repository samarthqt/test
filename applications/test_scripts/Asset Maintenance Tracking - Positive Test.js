describe('Asset Maintenance Tracking - Positive Test', () => {  const assetManagementPage = new AssetManagementPage();  const alertsPage = new AlertsPage();  const loginPage = new LoginPage();  before(() => {    cy.visit('/login');    loginPage.login('username', 'password');  });  it('should log and track maintenance tasks and receive alerts', () => {    assetManagementPage.navigateToAssetManagement();    assetManagementPage.selectAssetById('A123');    assetManagementPage.addMaintenanceTask('Oil Change', '2023-12-01', true);    alertsPage.navigateToAlertsSection();    alertsPage.verifyAlertScheduled('Oil Change');    loginPage.logout();    cy.wait(10000); // Wait for alert to trigger    loginPage.login('username', 'password');    alertsPage.checkAlertNotifications('Oil Change');    alertsPage.acknowledgeAlert('Oil Change');    assetManagementPage.verifyMaintenanceTaskInHistory('Oil Change');  });  after(() => {    loginPage.logout();  });});
describe('User Photo Upload Functionality', () => {  before(() => {    // Log in as a user with purchased products    cy.login('user@example.com', 'password');  });  it('should allow users to upload photos of purchased products', () => {    const productPage = new ProductPage();    const photoFilePath = 'path/to/sample-photo.jpg';    // Navigate to the product page of a purchased item    productPage.navigateToPurchasedProduct();    // Click on the 'Upload Photo' button    productPage.clickUploadPhotoButton();    // Select a valid photo file from the local drive    productPage.selectPhotoFile(photoFilePath);    // Click on 'Submit' to upload the photo    productPage.submitPhotoUpload();    // Verify the uploaded photo appears under the product's user gallery    productPage.verifyPhotoInGallery();  });});
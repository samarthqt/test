describe('Balance Inquiry Visibility Test', () => { before(() => { cy.visit('/login'); cy.get('#username').type('12345'); cy.get('#password').type('password'); cy.get('#loginButton').click(); }); it('should navigate to Account Management section', () => { cy.get('#accountManagementLink').click(); cy.url().should('include', '/account-management'); }); it('should select Balance Inquiry option', () => { cy.get('#balanceInquiryLink').click(); cy.url().should('include', '/balance-inquiry'); }); it('should display the correct current balance', () => { cy.get('#currentBalance').should('have.text', '$1000.00'); }); it('should verify the currency format and correctness of the balance', () => { cy.get('#currentBalance').invoke('text').then((text) => { expect(text).to.match(/^\$\d{1,3}(,\d{3})*(\.\d{2})?$/); }); }); after(() => { cy.get('#logoutButton').click(); cy.url().should('include', '/login'); }); });
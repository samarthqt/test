describe('Lease Agreement Template Selection', () => { beforeEach(() => { cy.visit('/login'); cy.loginAsLandlord(); }); it('should select a lease agreement template based on property type and location', () => { const dashboardPage = new DashboardPage(); const templatesPage = new TemplatesPage(); const leaseAgreementPage = new LeaseAgreementPage(); dashboardPage.navigateToTemplates(); templatesPage.selectPropertyType('Residential'); templatesPage.selectLocation('New York'); templatesPage.chooseTemplateById('T45678'); templatesPage.reviewTemplateDetails(); templatesPage.selectTemplateForUse(); leaseAgreementPage.createNewLeaseWithTemplate(); leaseAgreementPage.saveAndFinalizeLease(); leaseAgreementPage.verifyLeaseIsActive(); }); afterEach(() => { cy.logout(); }); });
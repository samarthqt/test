class DriverMonitoringPage { visit() { cy.visit('/driver-monitoring'); } enterDriverId(driverId) { cy.get('#driverId').type(driverId); } enterVehicleId(vehicleId) { cy.get('#vehicleId').type(vehicleId); } startDriving() { cy.get('#startDriving').click(); } checkStatus(expectedStatus) { cy.get('#status').should('contain', expectedStatus); } increaseSpeed(speed) { cy.get('#increaseSpeed').type(speed); } accelerate(speed) { cy.get('#accelerate').type(speed); } checkLogs(expectedLog) { cy.get('#logs').should('contain', expectedLog); } checkAlerts() { cy.get('#alerts').should('not.exist'); } maintainSpeed(speed) { cy.get('#maintainSpeed').type(speed); } decelerate() { cy.get('#decelerate').click(); } }
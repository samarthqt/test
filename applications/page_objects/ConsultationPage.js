class ConsultationPage { visitPatientRecord(patientId) { cy.visit(`/patients/${patientId}`); } createHealthPlan() { cy.get('[data-cy=create-health-plan]').click(); } enterHealthPlanDetails(healthPlan, planDuration, followUpFrequency) { cy.get('[data-cy=health-plan-name]').type(healthPlan); cy.get('[data-cy=plan-duration]').type(planDuration); cy.get('[data-cy=follow-up-frequency]').type(followUpFrequency); } reviewAndSubmitHealthPlan() { cy.get('[data-cy=review-health-plan]').click(); cy.get('[data-cy=submit-health-plan]').click(); } verifyHealthPlanSubmission() { cy.get('[data-cy=confirmation-message]').should('contain', 'Health plan is successfully submitted and pending approval'); cy.get('[data-cy=health-plan-status]').should('contain', 'activated'); } }